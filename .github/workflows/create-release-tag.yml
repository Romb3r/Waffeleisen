name: Create Release Tag

on:
  workflow_dispatch:
    inputs:
      tag_message:
        description: 'Tag message (optional)'
        required: false
        default: 'Release version'

jobs:
  create-release-tag:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
        ref: main

    - name: Configure Git
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"

    - name: Calculate new version
      id: new_version
      run: |
        # Read current version
        if [ -f version.txt ]; then
          current_version=$(cat version.txt)
          echo "Current version: $current_version"
        else
          current_version="1.0.0"
          echo "No version.txt found, starting with: $current_version"
        fi
        
        # Parse version components
        IFS='.' read -r major minor patch <<< "$current_version"
        
        echo "Creating major release - incrementing major version and resetting minor/patch to 0"
        
        # Increment major version and reset minor/patch to 0
        major=$((major + 1))
        minor=0
        patch=0
        
        new_version="$major.$minor.$patch"
        echo "New version: $new_version"
        echo "$new_version" > version.txt
        echo "version=$new_version" >> $GITHUB_OUTPUT

    - name: Commit version update
      run: |
        git add version.txt
        git commit -m "Release version ${{ steps.new_version.outputs.version }} [major]"
        git push origin main

    - name: Create release tag
      run: |
        new_version="${{ steps.new_version.outputs.version }}"
        tag_message="${{ github.event.inputs.tag_message }}"
        
        if [ -z "$tag_message" ]; then
          tag_message="Release version $new_version"
        fi
        
        git tag -a "v$new_version" -m "$tag_message $new_version"
        git push origin "v$new_version"
        
        echo "Created tag v$new_version with message: $tag_message $new_version"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Summary
      run: |
        echo "🎉 Release tag created successfully!"
        echo "📋 New version: ${{ steps.new_version.outputs.version }}"
        echo "🏷️ Tag: v${{ steps.new_version.outputs.version }}"
        echo "📝 Type: major (incremented major version, reset minor/patch to 0)"